import SettingsSection from "@/components/admin/SettingsSection";

export const dynamic = "force-dynamic";

export default function CountdownSettingsPage() {
  return <SettingsSection groupId="countdown" />;
}
